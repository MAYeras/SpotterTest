# Historical experiments

These scripts preserve the original work for context. Their splits and settings differ; do not use their metrics as a controlled comparison. Paths now resolve to the repository root. Some scripts overwrite prediction/model files when executed. Use `freight.py` for the supported evaluation and submission workflow.
